# Migration Status

| Domain | Phase | Notes |
| --- | --- | --- |
| frontend/src/app/ | In progress | Updated chat layout CSS so the agent thread column uses the composer width, keeping user message width aligned with the composer instead of spilling wider than it. Added prompt minimap marker/popup styles in `frontend/src/app/styles/globals/chat.css` for the agent timeline prompt navigation feature. |
| controller/src/modules/ | Not changed this turn | No migration-target changes made in this domain. |
| shared/src/ | Not changed this turn | No migration-target changes made in this domain. |
| cli/src/ | Not changed this turn | No migration-target changes made in this domain. |
